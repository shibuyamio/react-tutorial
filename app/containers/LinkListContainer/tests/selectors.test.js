// import { fromJS } from 'immutable';
// import { makeSelectLinkListContainerDomain } from '../selectors';

// const selector = makeSelectLinkListContainerDomain();

describe('makeSelectLinkListContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
